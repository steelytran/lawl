#include <dos.h>
#include <stdlib.h>
#include <conio.h>

/*
void __interrupt initsound(int interrupt, int value)
{
	outp(0x388, interrupt);
	delay(1);
	outp(0x389, value);
	delay(1);
}

void fmaudio(int hz)
{
	outp(0xA0, (div>>8)&0xFF);
	outp(0x40, div & 0xFF);
}*/
